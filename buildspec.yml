version: 0.2

phases:
  install:
    commands:
      - echo 'Hello World (INSTALL phase)'
  pre_build:
    commands:
      - echo 'Hello World (PRE_BUILD phase)'
  build:
    commands:
      - echo 'Hello World (BUILD phase)'
  post_build:
    commands:
      - aws s3 sync --exact-timestamps ./ s3://codepipeline-ap-northeast-1-833776994548